# Telegram Shell Bot 测试用例

---

## 一、基础功能

| # | 输入 | 预期结果 |
|---|------|----------|
| 1 | `uname -a` | 返回系统信息，代码块格式 |
| 2 | `whoami` | 返回 `root` |
| 3 | `pwd` | 返回 `/root`（初始目录） |
| 4 | `ls -la` | 列出文件，代码块格式 |
| 5 | `echo hello world` | 返回 `hello world` |
| 6 | `cat /etc/hostname` | 显示主机名 |

## 二、cd 持久化

| # | 输入 | 预期结果 |
|---|------|----------|
| 7 | `cd /var/log` | 返回 `📂 /var/log` |
| 8 | `pwd` | 返回 `/var/log`（验证持久化） |
| 9 | `ls` | 列出 /var/log 下的文件 |
| 10 | `cd /tmp` | 返回 `📂 /tmp` |
| 11 | `/cwd` | 返回 `📂 当前目录: /tmp` |
| 12 | `cd ~` | 返回 `📂 /root`（~ 展开测试） |
| 13 | `cd ..` | 返回上级目录 |
| 14 | `cd /nonexistent` | 返回 `❌ 目录不存在` |

## 三、交互式命令拦截

| # | 输入 | 预期结果 |
|---|------|----------|
| 15 | `top` | ⚠️ 拦截，提示用 `top -bn1` |
| 16 | `vim` | ⚠️ 拦截，提示用 cat/sed |
| 17 | `less /etc/passwd` | ⚠️ 拦截，提示用 cat |
| 18 | `htop` | ⚠️ 拦截 |
| 19 | `ssh root@localhost` | ⚠️ 拦截 |
| 20 | `tail -f /var/log/syslog` | ⚠️ 拦截，提示 `tail -n 50` |

## 四、非交互模式放行

| # | 输入 | 预期结果 |
|---|------|----------|
| 21 | `top -bn1` | ✅ 正常执行，显示进程列表 |
| 22 | `top -bn1 \| head -5` | ✅ 只显示前 5 行 |
| 23 | `python3 -c "print(1+1)"` | ✅ 返回 `2` |
| 24 | `python3 script.py` | ✅ 正常执行脚本 |
| 25 | `node -e "console.log('hi')"` | ✅ 返回 `hi` |
| 26 | `man -P cat ls` | ✅ 显示 man 页面 |

## 五、自动修正

| # | 输入 | 预期结果 |
|---|------|----------|
| 27 | `top -b` | 💡 提示自动修正为 `top -bn1`，然后执行 |

## 六、管道拦截

| # | 输入 | 预期结果 |
|---|------|----------|
| 28 | `cat /etc/passwd \| less` | ⚠️ 拦截，提示用 cat |
| 29 | `cat /etc/passwd \| head -3` | ✅ 正常返回前 3 行 |

## 七、超时保护

| # | 输入 | 预期结果 |
|---|------|----------|
| 30 | `sleep 5` | ✅ 5 秒后返回 |
| 31 | `sleep 60` | ❌ 超过 30 秒被终止 |
| 32 | `while true; do echo hi; done` | ❌ 被终止（timeout 兜底） |

## 八、斜杠命令

| # | 输入 | 预期结果 |
|---|------|----------|
| 33 | `/help` | 显示帮助信息 |
| 34 | `/cwd` | 显示当前目录 |
| 35 | `/random` | 返回 `未知命令` 提示 |

## 九、特殊输入

| # | 输入 | 预期结果 |
|---|------|----------|
| 36 | （空消息/图片） | 无响应 |
| 37 | `ls 特殊字符路径` | 正常处理 |
